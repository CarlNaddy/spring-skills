# Feature spec: <entity> CRUD

## Summary

Implement end-to-end CRUD operations for `<entity>`.

## Scope

### In scope

- Create `<entity>`
- Read/list `<entity>`
- Update `<entity>`
- Delete `<entity>`

### Out of scope

- Bulk operations
- Complex analytics

## Acceptance criteria

- [ ] Create flow works and validates inputs
- [ ] List/read flow renders correctly
- [ ] Update flow preserves existing constraints
- [ ] Delete flow includes confirmation and safe fallback

